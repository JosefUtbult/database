[package]
name = "database"
version = "0.1.0"
edition = "2024"

[dependencies]
database_macro = { path = "./database_macro/" }

mutex-traits = "1.0.1"
heapless = "0.9.2"

[dev-dependencies]
critical-section = { version = "1.2.0", features = ["std"] }
mutex = { version = "1.0.2", features = ["std"] }
